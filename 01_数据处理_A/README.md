# A 组数据处理区

本目录用于完成第 1 问的数据整理与规律分析。

## 目录用途

- `00_启动对齐/`：统一口径记录、阶段 0 任务拆分表
- `exports/`：清洗后的总表、维护匹配表、统计结果
- `figures/`：趋势图、季节图、维护前后对比图

## 当前阶段进度

| 阶段 | 状态 | 说明 |
| ---- | ---- | ---- |
| 阶段 0：启动对齐 | 已完成 | 已完成统一口径、任务拆分和阶段说明 |
| 阶段 1：数据盘点 | 已完成 | 已完成附件结构盘点、维护记录概览和异常情况登记 |
| 阶段 2：数据清洗 | 已完成 | 已输出 `clean_data.xlsx`、`maintenance_record.xlsx` 和 `数据清洗说明.md` |
| 阶段 3：第 1 问分析 | 已完成 | 已输出趋势图、季节性图、维护匹配表、维护效果统计表、下降率表和结论要点 |
| 阶段 4：辅助 B 建模 | 已完成首版交接 | 已生成 A → B 数据与建模参数交接说明，等待 B 反馈 |
| 阶段 5：辅助 C 论文整合 | 已完成首版交接 | 已生成 A → C 第 1 问论文素材交接说明，等待 C 反馈 |

## 当前已交付内容

### 阶段 0

- `00_启动对齐/统一口径记录.md`
- `00_启动对齐/A_任务拆分表.md`
- `00_启动对齐/README.md`

### 阶段 1

- `exports/数据结构说明表.md`
  - 说明附件 1、附件 2 的字段结构、记录数、日期范围、字段重命名建议和可直接使用性。
- `exports/维护记录概览表.md`
  - 说明每台过滤器的维护次数、维护类型分布、日期范围和维护匹配可行性。
- `exports/异常情况清单.md`
  - 记录缺失值、异常值、断档、维护匹配缺口和建议处理方式。
- `exports/阶段1_过滤器盘点统计表.csv`
  - 每台过滤器的记录数、缺失值、重复值、时间范围、间隔断档统计。
- `exports/阶段1_维护记录统计表.csv`
  - 每台过滤器的中维护/大维护次数、维护日期范围和维护间隔统计。
- `exports/阶段1_维护匹配可行性检查表.csv`
  - 检查维护记录在 `24h/72h/168h` 窗口下能否匹配到维护前后透水率。
- `exports/阶段1_透水率异常值统计表.csv`
  - 按 IQR 规则给出各过滤器异常值范围和异常点数量。

### 阶段 2

- `exports/clean_data.xlsx`
  - 清洗后的透水率总表，保留 `time`、`date`、`per`、`day_index`、`hour_index`、`season`、缺失标记和异常标记。
- `exports/maintenance_record.xlsx`
  - 清洗后的维护记录表，统一为 `filter_id`、`date`、`maintain_type`、`maintain_level` 等标准字段。
- `exports/数据清洗说明.md`
  - 记录字段说明、清洗规则、异常保留方式、统计结果和复现命令。

### 阶段 3

- `exports/maintenance_match.xlsx`
  - 维护记录与透水率匹配表，包含维护前后透水率、恢复量、恢复率、维护后 3/7/15 天均值和备注说明。
- `exports/维护效果统计表.xlsx`
  - 按维护类型和过滤器汇总维护恢复量、恢复率、维护后衰减率和维护有效性系数。
- `exports/每台过滤器下降率表.xlsx`
  - 包含每台过滤器的维护周期平均下降率、净趋势斜率、月平均透水率、季节平均透水率和影响系数。
- `exports/第1问结论要点.md`
  - 汇总第 1 问关于趋势、周期性、维护影响和可供 B 使用指标的文字结论。
- `exports/A_阶段0-3纵览检查与优化记录.md`
  - 从成员 A 视角记录阶段 0 至阶段 3 的完整性检查、质量判断、优化项和剩余风险。
- `figures/fig_*.png`
  - 第 1 问全部 9 张论文可用图表，已同步复制到 `03_论文_C/图表汇总/`。
  - `fig_01_all_filters_time_series.png` 保留为总体总览图；正文趋势分析优先使用 `fig_01a_filter_time_series_facets.png`，相对趋势说明使用 `fig_01b_normalized_permeability_trend.png`。

### 阶段 4 首版交接

- `exports/A_to_B_数据与建模参数交接说明.md`
  - 面向成员 B 的数据文件、字段说明、数据范围、建模参数和异常处理建议。

### 阶段 5 首版交接

- `exports/A_to_C_第1问论文素材交接说明.md`
  - 面向成员 C 的第 1 问图表清单、图注建议、表格清单、论文语言结论和写作建议。

## 复现方式

- 阶段 1 入口脚本：`04_代码/A_data_process.py`
- 运行命令：`python 04_代码/A_data_process.py`
- 当前脚本已实现阶段 1、阶段 2 和阶段 3，可重复生成 `exports/`、`figures/` 和 `03_论文_C/图表汇总/` 下的结果。
- 当前阶段 3 会生成 9 张图，其中 `fig_01a` 与 `fig_01b` 用于优化原 `fig_01` 信息密度过高的问题。
- 交接说明文件由成员 A 根据当前输出整理，若 B/C 后续提出新需求，应在对应交接说明中追加修订记录。

## 推荐交付顺序

1. 完成阶段 0 启动对齐
2. 完成阶段 1 数据盘点
3. 完成阶段 2 数据清洗
4. 完成阶段 3 趋势、季节性和维护影响分析
5. 已完成阶段 4 首版 A → B 交接材料，后续按 B 反馈补充字段或异常说明
6. 已完成阶段 5 首版 A → C 图表说明和论文可用结论，后续按 C 反馈调整图注或正文表述

## README 维护规则

- 每完成一个阶段，都要在本 README 中补充阶段状态、交付物和下一步动作。
- 若 `exports/`、图表目录新增文件，需同步在对应目录的 README 中说明用途和生成方式。
